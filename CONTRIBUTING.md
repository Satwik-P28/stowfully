# Contributing

Keep new capabilities local-first, exportable, and covered by tests where they affect stored data.

1. Open an issue for substantial changes.
2. Run `npm install` and make a focused change.
3. Run `npm run check` and `npm audit`.
4. Describe privacy and migration impact in the pull request.
